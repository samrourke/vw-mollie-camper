import localFont from "next/font/local";

export const shrikhand = localFont({
  src: "../../public/fonts/Shrikhand-Regular.woff2",
  variable: "--font-retro",
  display: "swap",
  weight: "500",
});
